import { View } from "react-native";
import styled from "styled-components";

const StyledView = styled(View)({
  display: "flex",
  height: "160px",
  flexDirection: "row",
});

export { StyledView };
